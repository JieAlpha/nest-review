import { ExampleFilterFilter } from "./example-filter.filter";

describe("ExampleFilterFilter", () => {
    it("should be defined", () => {
        expect(new ExampleFilterFilter()).toBeDefined();
    });
});
