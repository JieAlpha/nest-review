import { ExamplePipelinePipe } from "./example-pipeline.pipe";

describe("ExamplePipelinePipe", () => {
    it("should be defined", () => {
        expect(new ExamplePipelinePipe()).toBeDefined();
    });
});
